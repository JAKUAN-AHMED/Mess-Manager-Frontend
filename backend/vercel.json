{
  "version": 2,
  "builds": [{ "src": "server.js", "use": "@vercel/node" }],
  "routes": [{ "src": "/(.*)", "dest": "server.js" }],
  "crons": [
    {
      "path": "/api/reports/cron/send-monthly-bills",
      "schedule": "30 18 * * *"
    }
  ]
}
